version: "3.8"

services:
  user-service:
    build:
      context: ./user-service
    ports:
      - "8081:8081"
    networks:
      - my-network
    environment:
      - MONGO_URI=mongodb://mongo:27017/db_even_booking

  event-service:
    build:
      context: ./event-service
    ports:
      - "8082:8082"
    networks:
      - my-network
    environment:
      - MONGO_URI=mongodb://mongo:27017/db_even_booking

  api-gateway:
    build:
      context: ./api-gateway
    ports:
      - "8080:8080"
    networks:
      - my-network
    depends_on:
      - user-service
      - event-service

  mongo:
    image: mongo:latest
    ports:
      - "27017:27017"
    networks:
      - my-network
    volumes:
      - mongo-data:/data/db

networks:
  my-network:
    driver: bridge

volumes:
  mongo-data: